version: '3.9'

services:
  laravel-app:
    build:
      context: .
      dockerfile: docker/Dockerfile
    image: laravel_mcp
    container_name: laravel_mcp
    ports:
      - "127.0.0.1:8777:80"
    env_file:
      - ./.env
    # volumes:
    #   - .:/var/www/html